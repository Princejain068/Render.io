{
  "name": "build-server",
  "version": "1.0.0",
  "description": "A basic build server for clone git repo in docker container and then build it. After Building code Store files in s3 server.",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Prince Jain",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.645.0",
    "dotenv": "^16.4.5",
    "mime-types": "^2.1.35"
  }
}
